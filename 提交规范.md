# 提交规范

> 本仓库提交信息统一使用 **中文**，格式遵循 [Conventional Commits](https://www.conventionalcommits.org/) 的简化版本。

---

## 1. 基本格式

```
类型(范围): 简短中文描述
```

- **类型**：必填，表示变更性质
- **范围**：可选，表示影响模块；跨模块改动可省略或选最主要的一个
- **描述**：必填，简明说明「做了什么」，使用中文；句末不加句号

### 示例

```
feat(backend): 新增库表分页查询与轨迹时间范围过滤
fix(import): 支持中文 AIS CSV 列名映射
refactor(ingest): 移除落盘调度模块并以上传完成放行入库
style(frontend): 调整顶栏标题字号
chore: 初始化 AIS Platform greenfield 脚手架
```

---

## 2. 类型（type）

| 类型 | 含义 | 典型场景 |
| --- | --- | --- |
| `feat` | 新功能 | 新 API、新页面、新 Worker 能力 |
| `fix` | 缺陷修复 | 解析错误、状态机 bug、UI 显示问题 |
| `refactor` | 重构 | 不改外部行为的结构调整、模块合并/拆分 |
| `style` | 样式/格式 | 布局、间距、排版；**不含**业务逻辑变更 |
| `chore` | 杂项 | 依赖升级、脚手架、配置模板、文档维护 |
| `docs` | 文档 | 仅修改 `docs/`、`README` 等说明 |
| `test` | 测试 | 新增或修改测试用例 |
| `perf` | 性能 | 明确以性能为目标的优化 |

不常用但允许：`build`（构建/CI）、`revert`（回滚某次提交）。

---

## 3. 范围（scope）

按本仓库模块选用，可随演进补充：

| 范围 | 说明 |
| --- | --- |
| `backend` | Flask 路由、services、Doris 访问、鉴权等 |
| `frontend` | Vue 页面、组件、样式、前端 API 封装 |
| `import` | CSV 上传、解析、原始入库相关 |
| `ingest` | 上传完成门控、文件状态流转、入库流水线 |
| `worker` | `workers/runner` 及处理模块调度 |
| `db` | `database/doris/schema.sql`、表结构变更 |

范围不是必填。例如全仓库初始化可用 `chore: …` 不带范围。

---

## 4. 描述写法

1. **用中文**，一句话说清变更目的，避免「修改代码」「更新文件」等空泛描述  
2. **动词开头**：新增、修复、移除、重构、优化、调整、支持、扩展  
3. **尽量 ≤ 50 字**；复杂变更在 PR / 正文补充，不必全堆在标题  
4. **一条提交只做一件事**；多文件但同一功能可放在一个提交  
5. **不要**在标题里写 issue 号以外的长篇说明（必要时写在 commit body）

### 推荐

```
feat(frontend): 导入页展示上传进度并支持批量 CSV 上传
fix(backend): 修复批量取消时未回滚已上传文件的问题
```

### 不推荐

```
update
fix bug
feat: 改了一些东西。
新增任务进度追踪、清洗统计与 Worker 重试能力，另外还顺手修了导入页的样式，更新了 config
```

---

## 5. 提交正文（可选）

需要补充背景、破坏性变更或测试说明时，在标题后空一行写正文：

```
refactor(ingest): 移除落盘调度模块并以上传完成放行入库

- 单文件上传完成后立即置为 raw_pending
- 批量上传在 finish 接口统一放行
- Worker 模块收敛为 raw_import / cleaning / snapshot
```

---

## 6. 分批提交建议

按**功能边界**拆分，便于 review 与回滚：

| 顺序 | 内容 |
| --- | --- |
| 1 | 后端数据层 / API |
| 2 | Worker / 流水线 |
| 3 | 前端页面与交互 |
| 4 | 样式微调（可单独 `style` 提交） |
| 5 | 文档、配置（`docs` / `chore`） |

同一功能的前后端若强依赖，可放在同一提交；否则优先小步提交。

---

## 7. 与本项目相关的约定

- **配置**：根目录 `config.yaml`，提交信息中可写「配置」但不提交密钥  
- **Worker 模块名**与代码一致：`raw_import`、`cleaning`、`archive_complete`、`snapshot`  
- **文件状态**等业务枚举变更，在描述中点明影响的阶段（如「入库」「清洗」）  
- **前端布局**：见 [`前端布局规范.md`](./前端布局规范.md)

---

## 8. Git 协作

### 8.1 先拉取，再推送

推送前必须先与远程同步，避免把本地历史直接盖到他人提交之上：

```bash
git fetch origin
git pull --rebase origin master   # 或当前分支名
# 本地编译 / 自测通过后
git push origin master
```

习惯：**开始改代码前先 pull 一次，push 前再 pull 一次**。

### 8.2 冲突只在本地解决

- **禁止**在 GitHub / GitLab 网页上点「Resolve conflicts」在线合并  
- **禁止**把带冲突标记（`<<<<<<<`）的文件提交并 push  
- 冲突必须在本地打开文件手工合并，确认能运行、测试通过后，再 commit 并 push  

推荐流程：

```bash
git pull --rebase origin master
# 若冲突：编辑文件 → git add <文件> → git rebase --continue
# 若 rebase 搞乱：git rebase --abort 后改用 merge，但仍只在本地解决
git push origin master
```

### 8.3 其它

- **不要**对 `master` / `main` 使用 `git push --force`，除非团队明确约定且仅针对自己的未共享分支  
- 多人协作时优先 **小步提交 + 先 pull 再 push**，减少大范围冲突  
- 仅文档改动也遵循同一套拉取/推送顺序  

---

## 9. 快速自检

提交前对照：

- [ ] 格式是否为 `类型(范围): 中文描述`  
- [ ] 描述是否说清了「为什么 / 做了什么」  
- [ ] 类型是否与 diff 一致（纯样式不要用 `feat`）  
- [ ] 是否混入了无关文件的改动  
- [ ] 句末是否误加了句号或英文标题  
- [ ] 是否已 **先 pull** 且 **本地解决完冲突** 再 push  
